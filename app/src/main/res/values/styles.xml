<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/text_content</item>
        <item name="android:textSize">@dimen/text_size_content</item>
        <item name="android:screenOrientation">portrait</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/black</item>
    </style>
    <style name="AppTheme.AppBarOverlayWhite" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>


    <style name="MatchParent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="WrapContent">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="Button.Primary" parent="Base.Widget.AppCompat.ActionButton">
        <item name="android:layout_height">@dimen/height_common_clickable_item</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">@dimen/text_size_title</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/bg_primary_clickable_radius</item>
    </style>

    <style name="LineEdge">
        <item name="android:background">#ddd</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1px</item>
    </style>
    <style name="LineDivide">
        <item name="android:background">#ddd</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">2px</item>
    </style>

    <style name="TextView.Right" parent="TextAppearance.AppCompat">
        <item name="android:layout_height">@dimen/height_common_clickable_item</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginRight">16dp</item>
        <item name="android:textSize">@dimen/text_size_content</item>
        <item name="android:textColor">@color/text_hint</item>
        <item name="android:gravity">center_vertical|right</item>
    </style>

    <style name="TextView.Left" parent="TextAppearance.AppCompat">
        <item name="android:background">@drawable/bg_white_clickable</item>
        <item name="android:paddingLeft">@dimen/padding_edge_common</item>
        <item name="android:paddingRight">@dimen/padding_edge_common</item>
        <item name="android:layout_height">@dimen/height_common_clickable_item</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">@dimen/text_size_content</item>
        <item name="android:textColor">@color/text_content</item>
        <item name="android:gravity">center_vertical|left</item>
    </style>

    <style name="TextView.Clickable" parent="TextAppearance.AppCompat">
        <item name="android:background">@drawable/bg_white_clickable</item>
        <item name="android:paddingLeft">@dimen/padding_edge_common</item>
        <item name="android:paddingRight">@dimen/padding_edge_common</item>
        <item name="android:drawableRight">@drawable/ic_next</item>
        <item name="android:layout_height">@dimen/height_common_clickable_item</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:drawablePadding">16dp</item>
        <item name="android:textSize">@dimen/text_size_content</item>
        <item name="android:textColor">@color/text_title</item>
        <item name="android:gravity">center_vertical|left</item>
    </style>

    <style name="TextView.Button" parent="TextAppearance.AppCompat">
        <item name="android:layout_height">@dimen/height_common_clickable_item</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">@dimen/text_size_content</item>
        <item name="android:gravity">center</item>
    </style>
    <style name="TextView.Button.White" parent="TextView.Button">
        <item name="android:background">@drawable/bg_white_clickable</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <!-- 自定义仿IOS的AlertDialog的样式 -->
    <style name="AlertDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <!-- ios dialog end -->
    <!-- ios dialog end -->
    <style name="AnimBottom" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/push_bottom_out</item>
    </style>

    <style name="MyDialogStyleBottom" parent="android:Theme.Dialog">
        <item name="android:windowAnimationStyle">@style/AnimBottom</item>
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 -->
    </style>
</resources>
